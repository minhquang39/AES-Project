an HTML element become a grid container when its display property is set to grid or inline-grid

Grid:

- Grid column
- Grid row
- Grid gap
  - Column gap
  - Row gap
  - Gap
- Grid line
  -Grid-temple-column/row
  -Grid justify-content:-space-evenly
  -space-around
  -space-between
  -start
  -end
